# Personal-machine-only home-manager config. Empty for now — this repo has
# always been single-profile/single-machine, so there's nothing to put here
# yet. It exists so home/default.nix can select a profile by name; the
# work-mac host (added in a later branch) will be the first to actually need
# one (e.g. a separate git identity, no gaming/chat casks).
{ }
