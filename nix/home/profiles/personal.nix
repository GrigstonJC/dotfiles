# Personal-profile home-manager config. Empty until something
# personal-specific belongs here (e.g. a personal git identity, once
# work-mac needs a different one). Exists so home/default.nix can select a
# profile by name.
{ }
